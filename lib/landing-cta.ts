/**
 * Primary marketing CTA — follows the global brand `primary` color.
 * Add `rounded-2xl`, `rounded-full`, and padding at the call site.
 */
export const landingBlueCtaClassName =
  "inline-flex items-center justify-center bg-primary text-base font-semibold text-primary-foreground shadow-sm transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2";
